const initialState = {
  items: [],
  isLoading: false
};

const characters = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};

export default characters;
